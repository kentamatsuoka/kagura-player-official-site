{
  "name": "kagura-player-official-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro check && astro build",
    "publish": "bash scripts/publish-pages.sh",
    "preview": "astro preview --host 127.0.0.1",
    "check": "astro check",
    "format": "prettier --write .",
    "lint:json": "node scripts/check-json.mjs"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.9",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
